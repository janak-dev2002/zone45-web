# Frontend Agent — Done (Handoff)

> Agent: Frontend (Claude Sonnet 4.6)
> Branch: `frontend-agent-branch`
> Completed: 2026-05-28
> Status: **COMPLETE — PR raised to main**

---

## Summary

All [FRONTEND] tasks are complete. The repository now contains a fully working
React 18 + TypeScript 5 + Vite 5 frontend with:
- 7 public pages pre-rendered via `vite-react-ssg` (SSG)
- A CSR-only admin panel in a separate lazy-loaded chunk
- Full API integration against all 23 backend endpoints
- Design system exactly matching the design handoff (dark theme, ZF45 tokens)
- `react-helmet-async` meta tags and Open Graph on every public page
- Sitemap generated at build time, robots.txt static

---

## Public Routes (SSG — pre-rendered at build time)

| Route | Page | Notes |
|-------|------|-------|
| `/` | Landing | Hero, Services, Stack, Why, CTA |
| `/work` | Portfolio grid | Filtered by tech stack, loads from API post-hydration |
| `/work/:slug` | Portfolio detail | Full Markdown body, tech stack, outcome stats |
| `/notes` | Blog listing | Tag-filtered, featured post card |
| `/notes/:slug` | Blog post | Full Markdown with rehype-highlight |
| `/about` | About | Founder bio, timeline, mission, agent workflow |
| `/contact` | Contact | Form with Turnstile CAPTCHA, 202 success state |

Dynamic routes (`/work/:slug`, `/notes/:slug`) are pre-rendered at build time **only if the API is reachable** during the build. The `ssgOptions.includedRoutes` function in `vite.config.ts` fetches slugs from the API. If the API is unavailable, only the static routes are pre-rendered; the dynamic ones still work via CSR (Nginx SPA fallback).

---

## Admin Routes (CSR only — lazy-loaded, separate chunk)

| Route | Component | Notes |
|-------|-----------|-------|
| `/admin/login` | `Login.tsx` | JWT auth with Turnstile, httpOnly cookie |
| `/admin` | Redirects to `/admin/portfolio` | |
| `/admin/portfolio` | `PortfolioList.tsx` | Table with status filter, pagination |
| `/admin/portfolio/new` | `PortfolioForm.tsx` | Create with image upload |
| `/admin/portfolio/:id` | `PortfolioForm.tsx` | Edit with image upload |
| `/admin/posts` | `PostsList.tsx` | Table with draft/published filter |
| `/admin/posts/new` | `PostForm.tsx` | Markdown editor with live preview |
| `/admin/posts/:id` | `PostForm.tsx` | Edit with preview toggle |
| `/admin/contact` | `ContactList.tsx` | Read-only, status filter |
| `/admin/contact/:id` | `ContactDetail.tsx` | Full message + metadata |

Auth is enforced by `RequireAuth.tsx` — calls `GET /api/auth/me`; redirects to `/admin/login` on 401.

---

## Build Output

```
frontend/dist/
├── index.html        # Landing (pre-rendered)
├── work.html         # Portfolio grid (pre-rendered)
├── notes.html        # Blog listing (pre-rendered)
├── about.html        # About (pre-rendered)
├── contact.html      # Contact (pre-rendered)
├── sitemap.xml       # Generated by scripts/generate-sitemap.mjs
├── robots.txt        # Static
├── favicon.svg
└── assets/
    ├── app-*.js          # Main bundle (~680 kB — includes all public pages)
    ├── AdminApp-*.js     # Admin chunk (~63 kB — lazy-loaded)
    ├── app-*.css
    └── *.woff2           # Self-hosted Inter + JetBrains Mono
```

The admin chunk is separate from the public bundle. Admin code only loads if the user navigates to `/admin/*`.

---

## Environment Variables Required

| Variable | Purpose | Default |
|----------|---------|---------|
| `VITE_API_URL` | API base URL (no trailing slash, ends in `/api`) | `/api` (same-origin) |
| `VITE_TURNSTILE_SITE_KEY` | Cloudflare Turnstile site key | `1x00000000000000000000AA` (dev test key, always passes) |

Set in the Nginx Dockerfile build args or the `.env` file at build time.

---

## Nginx/DevOps Integration Notes

1. **Build command**: `npm run build` in `frontend/` — runs `vite-react-ssg build` then `node scripts/generate-sitemap.mjs`
2. **Build context in nginx.Dockerfile**: `COPY frontend/` into the build stage; `WORKDIR /app/frontend`
3. **Output**: `/app/frontend/dist` — copy to `/usr/share/nginx/html`
4. **SPA fallback**: Nginx already has `try_files $uri $uri/ /index.html;` — handles CSR admin routes
5. **Admin routes**: `/admin/*` is served by `index.html` via the SPA fallback; Nginx does NOT need special handling
6. **SSG build needs API**: If the API is up during build, dynamic routes are pre-rendered. If not, they still work via CSR.
7. **Backend dependency installs**: The frontend TypeScript references `../shared-types/src/index.ts` → `../../backend/src/lib/schemas.ts`. The `backend/` and `shared-types/` `node_modules` must be installed before running `npm run build` in `frontend/`. The multi-stage Dockerfile should `npm ci` in all three directories, or use a workspace root.

**Recommended Dockerfile build sequence:**
```dockerfile
# Install backend deps (needed for shared-types TS resolution)
WORKDIR /app/backend
COPY backend/package*.json ./
RUN npm ci

# Install shared-types deps
WORKDIR /app/shared-types
COPY shared-types/package*.json ./
RUN npm ci

# Install and build frontend
WORKDIR /app/frontend
COPY frontend/package*.json ./
RUN npm ci
COPY frontend ./
COPY shared-types ../shared-types
COPY backend/src ../backend/src
COPY backend/package.json ../backend/package.json
COPY backend/tsconfig.json ../backend/tsconfig.json
RUN npm run build
```

---

## API Endpoints Consumed

All 23 endpoints from `shared/api-contracts.md` are wired in `src/lib/api.ts`.

**Public endpoints:**
- `GET /portfolio` — portfolio grid + SSG slug enumeration
- `GET /portfolio/:slug` — portfolio detail
- `GET /posts` — blog listing
- `GET /posts/:slug` — blog post
- `GET /posts/tags` — tag filter chips
- `POST /contact` — contact form submission

**Auth endpoints:**
- `POST /auth/login` — admin login
- `POST /auth/logout` — admin logout
- `GET /auth/me` — session check on admin load

**Admin endpoints:**
- Full CRUD for `/admin/portfolio`, `/admin/posts`
- Read-only for `/admin/contact`
- `POST /admin/uploads/sign` + direct R2 PUT for image uploads

---

## Known Issues / Risks

1. **Large main bundle**: The public JS bundle is ~680 kB unminified. This is due to all public pages being eagerly loaded. Consider code-splitting by route in a future iteration. Gzip reduces to ~210 kB, which is acceptable.

2. **SSG dynamic routes**: If the API is not reachable at build time (e.g., first deploy before DB migration), portfolio/post detail pages won't be pre-rendered. They still work via CSR.

3. **Turnstile in dev**: Use `VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA` for local dev. Replace with the real Cloudflare site key in production.

4. **React markdown**: Code highlighting uses `rehype-highlight` which includes many language definitions. If bundle size is a concern, use the slim version.

---

*End of frontend-done handoff. Frontend Agent session complete.*
